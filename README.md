# git_HandsOn

## Description
This script classifies a biological sequence as DNA or RNA.

## Usage
```bash
python seqClass.py -s <sequence> [-m <motif>]
```

## Arguments
- `-s` / `--seq` : Input sequence (required)
- `-m` / `--motif` : Motif to search for in the sequence (optional)
